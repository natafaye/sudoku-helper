
// All the sudoku digits for convenience
export const DIGITS = [1, 2, 3, 4, 5, 6, 7, 8, 9]

// The initial value for the sum when the app loads
export const INITIAL_SUM = "10-12, 14";

// The initial value for the number of squares when the app loads
export const INITIAL_NUM_SQUARES = 2;

// The bootstrap variant for excluded digits
export const EXCLUDED_VARIANT = "warning"

// The bootstrap variant for included digits
export const INCLUDED_VARIANT = "success"