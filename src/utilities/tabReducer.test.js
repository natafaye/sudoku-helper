
describe("tabReducer", () => {
    it("sets up the intitial tab list and current tab correctly", () => {

    })

    it("creates a new tab with the default data", () => {
        
    })

    it("creates a new tab with custom data", () => {
        
    })

    it("switches between tabs", () => {
        
    })

    it("updates the data for the current tab", () => {
        
    })

    it("deletes a tab from the end that is not the last tab", () => {
        
    })

    it("deletes a tab from the beginning that is not the last tab", () => {
        
    })

    it("deletes a tab from the middle that is not the last tab", () => {
        
    })

    it("deletes the last tab", () => {
        
    })
})